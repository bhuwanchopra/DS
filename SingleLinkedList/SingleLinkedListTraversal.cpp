/*
 * SingleLinkedListTraversal.cpp
 *
 *  Created on: Mar 28, 2014
 *      Author: bhuwan
 */

#include "SingleLinkedList.h"

template<class DataType>
vector<DataType> traversal(SingleLinkedList<DataType>& list) {
//	if (nullptr == list.head) {
//		return vector<DataType>();
//	}
	vector<DataType> ret;

//	for(auto n = list.head; nullptr != n; n = n->getNext()) {
//		ret.emplace_back(n->getData());
//	}
	return ret;
}
